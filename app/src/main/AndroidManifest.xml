<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ifty.myproducts">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/my_products_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/my_products_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".FavouriteActivity"></activity>
        <activity android:name=".OrderNotificationActivity" />

        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SetupActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".NewAdPostActivity" />
        <activity android:name=".ItemsActivity" />
        <activity android:name=".ProductAddActivity" />
        <activity android:name=".CustomerSetupActivity" />
        <activity android:name=".ProductDetailsActivity" />
        <activity android:name=".CompanyProfileActivity" />
        <activity
            android:name=".ProductOrderActivity"
            android:windowSoftInputMode="stateHidden|adjustResize" />
    </application>

</manifest>